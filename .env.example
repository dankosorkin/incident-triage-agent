OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional. Unset = /chat has no auth (fine for local dev only). Set
# before exposing the service anywhere reachable by anyone else, and
# send it back as the X-API-Key header.
SERVICE_API_KEY=
